services:
  geoguessr-worker:
    build:
      context: .
    command: ["python3", "-m", "apps.geoguessr_server"]
    stdin_open: true
    tty: true
    environment:
      GOOGLE_MAPS_API_KEY: "${GOOGLE_MAPS_API_KEY}"
      IMAGE_OUTPUT_DIR: /data/images
      SERVER_PORT: "8000"
    volumes:
      - geoguessr-images:/data/images
    shm_size: "1gb"
    restart: "no"
    ports:
      - "8000:8000"

volumes:
  geoguessr-images:
